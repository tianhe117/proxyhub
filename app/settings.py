"""Configuration persistence (reuse refact, drop multi-engine fields).

Paths are not hard-coded; resolved via settings + environment variables.
Concrete fields are deferred to the data/deployment layers.
"""
